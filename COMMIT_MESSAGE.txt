Fix assessment issues: memory leaks, async I/O, pagination, virtualization, and UI enhancements

- Fix memory leaks in Items and ItemDetail components using useRef mount tracking
- Refactor backend to use async/await for non-blocking file I/O operations
- Implement server-side pagination and search with debounced input
- Add react-window virtualization for performance with large lists
- Implement stats caching with file watcher for performance optimization
- Add Vite proxy configuration to eliminate CORS issues
- Modernize UI with gradient theme, improved spacing, and smooth animations
- Enhance UX with loading states, hover effects, and better visual hierarchy

